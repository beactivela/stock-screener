# Dependencies & build output (rebuilt in image)
node_modules
dist

# Secrets and local data
.env
.env.*
!.env.example
data

# VCS / editor
.git
.github

# Large local-only dirs (not imported by server at runtime)
regime_trading
.cursor
.vscode
*.log
.DS_Store
.vercel

# Tests & docs (not needed to run the app)
**/*.test.js
**/*.test.ts
**/*.test.tsx
coverage
eslint.config.js
