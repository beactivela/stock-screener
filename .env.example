# Massive API key (get from massive.com). Required for bars, ETF constituents, dividends.
# Never commit .env or expose the key in the frontend.
MASSIVE_API_KEY=your_api_key_here

# Supabase (optional). When set, app can use DB instead of flat JSON files.
# Get from Project Settings > API in Supabase dashboard.
# Use service_role key for server-side (bypasses RLS); anon for read-only.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_service_role_key_here

# For migrations (preferred - avoids URL parsing issues with special chars in password):
# SUPABASE_PROJECT_REF=ksnneoomyrvmzukwxmqg
# SUPABASE_DB_PASSWORD=your_db_password
# Or full URI: DATABASE_URL=postgresql://postgres:[PASSWORD]@db.ksnneoomyrvmzukwxmqg.supabase.co:5432/postgres

# Optional: run full VCP scan every 24 hours when the server is running
# SCHEDULE_SCAN=1

# Cron auth: required for POST /api/cron/scan (e.g. Supabase Cron at 5 PM CST). Use a long random string.
# CRON_SECRET=your_random_secret_here

# Optional: port when running API-only (npm run server). Dev uses 5173 via npm run dev.
# PORT=3001

# Vercel / production: set in Vercel project env vars so frontend fetches from external API
# VITE_API_URL=https://your-api.railway.app

# Minervini AI coach (in-app chat, Claude). Get key from console.anthropic.com.
# Optional: ANTHROPIC_CHAT_MODEL=claude-3-5-sonnet-20241022 (or another model id)
# ANTHROPIC_API_KEY=sk-ant-...
